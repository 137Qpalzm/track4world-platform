# 工作交接文档

> 最后更新: 2026-03-21 (阶段一4个遗留问题全部修复)
> 用途: 跨会话恢复工作进度

---

## 当前总进度

| 阶段 | 状态 | 完成度 |
|------|------|--------|
| 一：环境搭建与模型复现 | **已完成** | 100% |
| 二：评估与分析 | 未开始 | 0% |
| 三：展示平台搭建 | 未开始 | 0% |
| 四：论文撰写与答辩 | 未开始 | 0% |

---

## 阶段一详细状态

### 已完成
- [x] Conda 环境 `track4world` 搭建 (Python 3.11 + PyTorch 2.5.1+cu121)
- [x] Track4World 仓库克隆 + 全部依赖安装
- [x] 三个预训练权重下载 (da3/pi3/moge)
- [x] 三种模式 (2d/3d_ff/3d_efep) 推理成功验证
- [x] 代码结构梳理 → `docs/技术文档.md`
- [x] 项目规划文档 → `项目规划.md`
- [x] 一键展示脚本 → `stage1_showcase.py` (推理+环境检测正常工作)
- [x] HTML 报告 → `stage1_results/report.html`
- [x] 3D 点云离线截图 (open3d 渲染)

### 已修复 (4 个问题, 全部完成)

#### 问题 1: 2D GIF 可视化效果差 ✅ 已修复
- **原因**: 原始 GIF 全帧颜色编码，看不出追踪效果
- **修复**: `make_visuals.py` 中 `make_2d_motion_gif` 已实现:
  - 背景保持原始视频帧
  - 仅运动区域叠加追踪颜色 (光流差异检测运动)
  - 黄色轮廓标记运动边界
  - 自适应阈值 + 形态学处理去噪
- **之前报错原因**: 实为问题4的编码错误导致，修复编码后 GIF 生成正常

#### 问题 2: 3D 模式在 HTML 中展示不足 ✅ 已修复
- **原因**: open3d 离屏渲染对极小坐标范围 (0.1~0.27) 的点云几乎全黑; 且 open3d 无法打开含中文的绝对路径
- **修复**: `make_visuals.py` 中 `render_pointcloud_screenshots` 改用 matplotlib 3D 散点图渲染:
  - 先复制 PLY 到 ASCII 临时目录绕过中文路径
  - matplotlib 渲染不受坐标范围限制
  - 3d_ff 和 3d_efep 各 3 张截图 (首帧/末帧/场景流) 全部正常 (~143KB)
- **HTML 报告已更新**: 包含 6 张 3D 点云截图 + 详细说明

#### 问题 3: Viser 点云查看器连接问题 ✅ 已修复
- **排查结果**: vis_3d_ff.py 和 viser 服务器本身可以正常启动 (加载 10 帧仅需 ~1.6s)
- **原问题原因**: 可能是端口冲突 (8080 被占用)、浏览器缓存、或首次加载未等待足够时间
- **修复**: 重写了 `simple_viewer.py` 作为增强版查看器:
  - 自动复制 PLY 到 ASCII 临时目录 (绕过潜在中文路径问题)
  - 支持 3d_ff 和 3d_efep 两种模式
  - 降采样加速渲染 (默认 1/4)
  - 退出时自动清理临时文件
  - 使用独立端口 8012 避免冲突
- **使用**: `E:/Conda/envs/track4world/python.exe simple_viewer.py --mode 3d_ff`

#### 问题 4: GIF 生成编码错误 ✅ 已修复
- **原因**: Windows 下 subprocess 默认编码为 GBK，子进程输出中文时崩溃
- **修复**: `stage1_showcase.py` 中 `run_in_t4w_env` 和 `run_inference` 两处 subprocess 调用添加 `PYTHONIOENCODING=utf-8` 环境变量 + `encoding='utf-8', errors='replace'` 参数

---

## 关键环境信息

```
Conda 环境:    track4world
Python 路径:   E:/Conda/envs/track4world/python.exe
系统 Python:   C:/Python312/python.exe (3.12) ← 不要用这个跑模型!
GPU:           RTX 3050 Laptop 4GB VRAM
VRAM 限制:     只能用 camera_base + moge 权重
HF 镜像:       HF_ENDPOINT=https://hf-mirror.com
```

## 关键命令

```bash
# 激活环境
conda activate track4world
# 或直接用绝对路径
T4W="E:/Conda/envs/track4world/python.exe"

# 2D 追踪推理
$T4W demo.py --mp4_path demo_data/cat.mp4 --mode 2d --image_size 320 --max_frames 20

# 3D 追踪推理 (必须用 camera_base + moge)
$T4W demo.py --mp4_path demo_data/cat.mp4 --mode 3d_ff \
  --coordinate camera_base --ckpt_init checkpoints/track4world_moge.pth \
  --image_size 448 --max_frames 10

# 点云可视化 (viser, 浏览器打开 localhost:8080)
$T4W visualization/vis_3d_ff.py --ply_dir results/cat_3dff_base/3d_ff_output

# 一键展示 (任意 python 均可)
python stage1_showcase.py
```

---

## 下一步工作建议

### 优先级 1: 修复阶段一的 4 个问题
让展示效果达到可用水平，便于答辩演示。

### 优先级 2: 开始阶段二 — 评估与分析
- 下载评估数据集 (参考 `Track4World/evaluation/eval.md`)
- 在 demo 数据上做定性分析 (多个视频对比)

### 优先级 3: 开始阶段三 — Gradio 平台
- 基于 demo.py 封装推理接口
- 搭建 Gradio 前端 (视频上传 → 推理 → 结果展示)

---

## 文件索引

| 文件 | 说明 |
|------|------|
| `项目规划.md` | 总体规划 (四个阶段, 目录结构, 风险) |
| `docs/技术文档.md` | 模型架构+代码结构+维度速查 |
| `stage1_showcase.py` | 一键展示脚本 (环境检测+推理+可视化) |
| `stage1_results/report.html` | HTML 展示报告 |
| `stage1_results/results.json` | 推理结果元数据 |
| `make_visuals.py` | 可视化生成脚本 (2D GIF + 3D 截图) |
| `Track4World/demo.py` | 模型推理入口 (已修改 float16) |
